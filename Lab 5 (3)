import numpy as np
import matplotlib.pyplot as plt
N=10
x = np.zeros(N)
y = np.zeros(N)
for i in range(1,N):
    x[i] = x[i-1] + np.random.uniform(-1,1)
    y[i] = y[i - 1] + np.random.uniform(-1, 1)
plt.plot(x,y)
plt.show()
